{
  "name": "Lab-8a Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspace/.venv/bin/python",
        "terminal.integrated.shell.linux": "/bin/bash",
        "editor.formatOnSave": true
      }
    }
  },
  "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip setuptools wheel && if [ -f requirements.txt ]; then . .venv/bin/pip install -r requirements.txt; fi && if [ -f setup.py ]; then . .venv/bin/pip install -e .; fi && . .venv/bin/pip install pytest pytest-cov",
  "forwardPorts": [8000],
  "remoteUser": "vscode"
}
